- Consider a broader benchmark that exercises `Mint.HTTP1.stream/2` end-to-end on a synthetic response corpus. The current microbenchmark is useful, but a fuller decode loop may reveal wins in `lib/mint/http1.ex` state transitions and buffering that don't show up yet.
- Explore generated/common-header validation tables or generated function clauses for a larger set of standard HTTP request headers, but only if code size stays reasonable. The exact-name fast paths were a large win, so a principled expansion may pay off.
- Revisit manual response-header parsing only if done with a carefully correct remainder model and strong tests around `101 Switching Protocols`, pipelining, and header-end handling. The naive exact-prefix attempt was both slower and incorrect, but a better design could still beat `:erlang.decode_packet/3` for a narrow subset of common headers.
