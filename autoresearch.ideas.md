- Consider a broader benchmark that exercises `Mint.HTTP1.stream/2` end-to-end on a synthetic response corpus. The current microbenchmark is useful, but a fuller decode loop may reveal wins in `lib/mint/http1.ex` state transitions, buffering, and header/body handoff that don't show up yet.
- Explore broader generated/common-header encode clauses only if they stay generic and code size remains reasonable. The request-path speedups are now mostly coming from skipping generic validation/dispatch on common headers, so a carefully bounded expansion of those clauses may still pay off.
